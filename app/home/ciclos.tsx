import { View, Text } from "react-native";

export default function Principal() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Essa é a aba de ciclos</Text>
    </View>
  );
}
